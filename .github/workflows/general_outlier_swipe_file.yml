name: General outlier swipe file

on:
  workflow_dispatch:

jobs:
  write-swipe-file:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Write General Long Form Outliers data
        run: python outlier-tracking/general-long-form/general_outlier_swipe_file.py

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add outlier-tracking/general-long-form/data.json
          git diff --cached --quiet || git commit -m "Update general long form outlier data [skip ci]"
          git push
